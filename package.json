{
    "name": "Rapid Stream",
    "version": "0.0.0",
    "scripts": {
      "ng": "cd fe && ng",
      "start": "ng serve",
      "watch": "ng build --watch --configuration development",
      "test": "ng test",
      "docker:dev": "npm run docker:down && cd docker && docker-compose up -d",
      "docker:prod": "cd docker && npm run docker:down --remove-orphans && docker-compose -f docker-compose.prod.yml up -d",
      "docker:site": "docker exec -it container-site bash",
      "docker:down": "cd docker && docker-compose down --remove-orphans",
      "deploy": "npm run build-angular && npm run build-express && npm run move-fe && npm run start-express",
      "build-angular": "cd fe && ng build --configuration production",
      "build-express": "cd be && npm run build",
      "move-fe": "rm -rf /var/www/* && cp -r fe/dist/* /var/www/",
      "start-express": "cd be && npm run start-prod"
    }
  }
  